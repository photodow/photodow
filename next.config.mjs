/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    // basePath: "/github-pages",
    reactStrictMode: true,
    // distDir: "dist",
};

export default nextConfig;
